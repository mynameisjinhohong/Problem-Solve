#include <iostream>
#include <iomanip>
#include <string>
#include <vector>
#include <cmath>
#include <vector>
#include <list>

using namespace std;

int main()
{
	int a;
	cin >> a;
	for (int i = 0; i < a; i++) {
		int b,c;
		cin >> b >> c;
		if (c > b) {
			cout << "NO BRAINS" << endl;
		}
		else {
			cout << "MMM BRAINS" << endl;
		}
	}

}
